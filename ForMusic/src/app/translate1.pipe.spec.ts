import { Translate1Pipe } from './translate1.pipe';

describe('Translate1Pipe', () => {
  it('create an instance', () => {
    const pipe = new Translate1Pipe();
    expect(pipe).toBeTruthy();
  });
});
